// exports.showPrescriptionList = (req, res, next) => {
//   if(req.session.login){
//   res.render("prescriptions", { navLocation: "prescriptions" });
//   } else{
//     res.redirect('..');
//   }
// };

// exports.showAddPrescriptionForm = (req, res, next) => {
//   if(req.session.login){
//   res.render("prescriptionAdd", { navLocation: "prescriptions" });
// } else{
//   res.redirect('..');
// }
// };

// exports.showPrescriptionDetails = (req, res, next) => {
//   if(req.session.login){
//   res.render("prescriptionAdd", { navLocation: "prescriptions" });
// } else{
//   res.redirect('..');
// }
// };
